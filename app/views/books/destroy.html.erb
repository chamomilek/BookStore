<h1><%= @book.title %></h1>

<p><%= @book.description %></p>

<ul>
  <li><%= link_to "Destroy", controller: "books", category_id: @book.category_id, action: "destroy", data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>
</ul>