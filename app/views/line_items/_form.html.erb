<%= form_with(model: line_item) do |form| %>
  <% if line_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
        <% line_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :cart, style: "display: block" %>
    <%= form.text_field :cart %>
  </div>

  <div>
    <%= form.label :belongs_to, style: "display: block" %>
    <%= form.text_field :belongs_to %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
